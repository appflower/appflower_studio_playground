propel:
  af_guard_user_profile:
    _attributes: { phpName: afGuardUserProfile }
    user_id: { type: integer, primaryKey: true, foreignTable: af_guard_user, foreignReference: id, onDelete: cascade }
    timezones_id: { type: integer, foreignTable: timezones, foreignReference: id, onDelete: cascade }
    first_name: varchar(20)
    last_name: varchar(20)
    job_title: varchar(255)
    phone_mobile: varchar(255)
    phone_office: varchar(255)
    personal_body: longvarchar
    image: varchar(255)
    is_developer: boolean
    widget_help_is_enabled: { type: boolean, default: true }
    created_at: null
    updated_at: null
  changelog:
    _attributes: { idMethod: native, phpName: Changelog }
    id: { type: INTEGER, required: true, autoIncrement: true, primaryKey: true }
    object_id: { type: INTEGER, required: true, default: 0 }
    object_name: { type: VARCHAR, size: 255, required: true, default: null }
    object_change_type: { type: VARCHAR, size: 2, required: true, default: 0 }
    object_link: { type: VARCHAR, size: 255 }
    module_name: { type: VARCHAR, size: 255, required: true, default: null }
    changes: { type: LONGVARCHAR, required: true }
    user_commit_msg: { type: LONGVARCHAR, required: true }
    user_id: { type: INTEGER, required: true, default: 0, foreignTable: sf_guard_user, foreignReference: id, onDelete: cascade }
    updated_at: { type: TIMESTAMP }
  timezones:
    _attributes: { phpName: TimeZones }
    id: null
    name: { type: varchar(128), index: true }
    offset: { type: integer }
    created_at: null
    updated_at: null
  newmodel:
    _attributes: { phpName: NewModel }
  aaaaaaaaafe:
    _attributes: { phpName: AAAAAAAAAFe }
  qrwa:
    _attributes: { phpName: qrwa }
  huj:
    _attributes: { phpName: huj }
  hmmm:
    _attributes: { phpName: hmmm }
  newmodely:
    _attributes: { phpName: NewModely }
  newmodelo:
    _attributes: { phpName: NewModelo }
